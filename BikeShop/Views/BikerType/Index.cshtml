@using BikeShop.Config
@model List<BikeShop.Models.BikerType>
@{
    ViewBag.Title = "Index";
}

<h2>Categorii de biciclisti</h2>

@Html.ActionLink("Adauga o categorie noua", Utilities.ACTION_NEW, Utilities.BIKER_TYPE_CONTROLLER, new { @class = "btn btn-primary" })
<br />
<br />

<table id="types" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Nume</th>
            <th>Descriere</th>
            <th>Actiuni</th>
        </tr>
    </thead>
    <tbody>
        @foreach (BikeShop.Models.BikerType p in Model)
        {
            <tr>
                <td>@p.Name</td>
                <td>
                    @p.Description
                    <br />
                    <br />
                    @Html.ActionLink("Mai multe detalii", Utilities.ACTION_DETAILS, new { id = p.BikerTypeId })
                </td>
                <td>
                    @Html.ActionLink("Modifica informatii", Utilities.ACTION_EDIT, new { id = p.BikerTypeId }, new { @class = "btn btn-primary" })
                    <br /><br />
                    <button class="btn-delete btn btn-danger" data-id="@p.BikerTypeId">Sterge</button>
                    <br />
                </td>
            </tr>
        }

    </tbody>

</table>

@section scripts
{
    <script>
        $(document).ready(function () {

            var table = $("#types").DataTable();

            $("#types").on("click", ".btn-delete", function () {

                var btnDelete = $(this);
                var typeId = btnDelete.attr("data-id");

                if (confirm("Esti sigur?")) {
                    $.ajax(
                        {
                            url: "@Utilities.BIKER_TYPE_CONTROLLER/@Utilities.ACTION_DELETE/" + typeId,
                            method: "POST",
                            success: function () {
                                console.log("[SUCCESS-delete] bikerTypeId = " + typeId);
                                table.row(btnDelete.parents("tr")).remove().draw();
                            },
                            error: function () {
                                console.error("[ERROR-delete] bikerTypeId = " + typeId);
                                table.row(btnDelete.parents("tr")).remove().draw();
                            }
                        }
                    );
                }
            });

        });
    </script>
}




